import React from 'react';
import './Graph3DSection.css';

export default function Graph3DSection({ input }) {
  return (
    <section className="graph-section fade-in">
      <h2>🧊 3D Model Rendering</h2>
    </section>
  );
}